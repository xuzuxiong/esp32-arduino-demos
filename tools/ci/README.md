# GitHub Actions CI 模板（可选）

本文件为 **参考用 CI 配置**，仓库默认不启用 Actions（避免额外 OAuth 权限依赖）。

- 占位 Broker / 编译 FQBN 与主仓库一致  
- 维护者若需启用：将此文件复制为 `.github/workflows/compile.yml` 并 push  

**普通使用者克隆仓库阅读/编译固件，无需任何 CI 相关操作。**
